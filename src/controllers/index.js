const { createCity, deleteCity, getCity, updateCity, getAllCity, searchCity } = require("./city-controller");

module.exports = {
    createCity,
    deleteCity,
    getCity,
    updateCity,
    getAllCity,
    searchCity
}